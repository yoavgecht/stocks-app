import React from 'react';
import loader from '../images/giphy_s.gif';

const Loader = (props) => (	
	<img src={loader} alt="loader img" className="center-block loader img-responsive"/>		
);

export default Loader;